const publicRoutes = {
  home: '/',
  login: '/login',
  register: '/register',
};

const privateRoutes = {
  managerCourt: '/admin/manager-court',
  calendar: '/admin/calendar',
};

export { publicRoutes, privateRoutes };
