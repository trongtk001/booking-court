'use client';
import { Navbar } from 'flowbite-react';

const Brand = Navbar.Brand;
const Link = Navbar.Link;
const Toggle = Navbar.Toggle;
const Collapse = Navbar.Collapse;

export default Navbar;
export { Brand, Link, Toggle, Collapse };
